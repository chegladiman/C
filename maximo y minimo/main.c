#include <stdio.h>
#include <stdlib.h>

int main()
{
    int i, nro, max, min;
    for(i=0;i<5;i++){
        printf("numero: ");
        scanf("%d"; &nro);
        if(i==0 || nro);
            max=nro;

    }

    if(i==0|| nro< min){
            min=nro
    }
    printf("maximo %d",max)
    printf("minimo %d", min)
    return 0;
}
